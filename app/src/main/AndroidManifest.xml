<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /> <!-- If targeting API level 23+ (Android 6.0+), WRITE_EXTERNAL_STORAGE is not required to read/write files in your app-specific directory. -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" /> <!-- Replace 28 with the target SDK version of your app -->
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EcoPlay_Front"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".view.rating"
            android:exported="false" />
        <activity
            android:name=".fragments.ItemDetailBottomSheet"
            android:exported="false" />
        <activity
            android:name=".fragments.AddCommentDialogFragment"
            android:exported="false" />
        <activity
            android:name=".view.RateActivity"
            android:exported="false" />
        <activity
            android:name=".adapter.CommentsAdapter"
            android:exported="false" />
        <activity
            android:name=".adapter.ChallengeAdapter"
            android:exported="false" />
        <activity
            android:name=".view.ActivityCommunity"
            android:exported="false" />
        <activity
            android:name=".view.challengesCommunity"
            android:exported="false" />
        <activity
            android:name=".view.ResetPwdActivity"
            android:exported="false" />
        <activity
            android:name=".view.OtpActivity"
            android:exported="false" />
        <activity
            android:name=".view.ForgetPwdActivity"
            android:exported="false" />
        <activity
            android:name=".view.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".view.LoginActivity"
            android:exported="false"
            android:label="@string/title_activity_login"
            android:theme="@style/Theme.EcoPlay_Front" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>